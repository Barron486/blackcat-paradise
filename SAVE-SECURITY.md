# 伺服器結算與存檔安全

## 線上角色的唯一來源

登入後，角色從伺服器 SQLite 資料庫載入。瀏覽器與 CLI 只送出「前往地圖、使用指定物品、購買、裝備、調整自動戰鬥設定」等操作；戰鬥、傷害、死亡、經驗、升級、金幣、掉寶與物品消耗都在伺服器執行遊戲規則後保存。

- `POST /api/game` 是操作入口，必須有登入、CSRF、帳號控制租約；修改還需要版本、角色欄位／世代與唯一請求 ID。
- `POST /api/sync` 一律拒絕，包含 GM。修改 LocalStorage、匯出 JSON、CLI checkpoint、客戶端 tick、價格或獎勵，不能覆蓋線上資料。
- 創角由伺服器建立，客戶端只能指定名稱、職業、性別、模式及合法配點。讀取／刪除使用角色世代，不能複製另一欄或重用已刪除角色。
- 商城、交易所與 GM 繼續使用各自的伺服器交易紀錄。戰鬥結算前讀取最新版本，避免復活已售出的物品、覆蓋購買效果或撤銷 GM 操作。
- 相同請求 ID 的網路重送只執行一次；同一 ID 不能換內容。失敗操作同時回滾資料庫與記憶體狀態。

## 時間、背景與重新連線

- 伺服器每秒推進遊戲，以伺服器單調時鐘計算 100 ms tick。反覆重整、加速輪詢或偽造客戶端時間不會增加結算速度。
- 網頁每秒取回結果；背景分頁只要持續連線就繼續戰鬥。最後一次有效連線後最多保留 90 秒寬限，之後暫停。超出的離線時間不補算。
- 重新連線保留 HP、MP、死亡、當前怪物與攻擊冷卻；不因重新載入免費回村、補血或重置攻擊間隔。
- 伺服器重啟從最後保存狀態恢復，未連線時不自動推演整段離線獎勵。使用者更新版本後須重新整理網頁；舊 CLI 必須更新。

## 執行與維護

`server/game-engine.mjs` 在隔離的 JSDOM 中執行既有規則；程式與遊戲資料由伺服器載入，不執行使用者提供的 JavaScript。`server/game-actions.mjs` 驗證操作白名單、庫存 UID、NPC、數量與選項；價格、材料、成功率和結果由伺服器決定。新增玩法也必須新增及測試伺服器操作，不得恢復整份存檔上傳。

部署需要 Node.js 24+、`npm ci --omit=dev` 及持久化 `/data`。目前使用單一 Node 程序、單一 SQLite 資料庫，最多 64 個同時載入的帳號模擬；擴充多副本前須加入跨程序的獨占結算機制。每個帳號只能控制一個角色。

## 保留資料與安全界線

- 遷移保留現有雲端角色，不重置等級、金幣或物品；無法追溯證明遷移前的每筆收益。
- 這阻擋「本機改檔後提交」與「客戶端自行宣告戰鬥收益」，不代表遊戲規則、NPC 經濟或所有應用程式邏輯永遠沒有漏洞。伺服器仍需持續更新與備份。
- 公開的存檔校驗碼僅供資料完整性檢查，不是授權依據；GM 權限與藍鑽餘額仍取自伺服器。
- 舊存檔 API 的拒絕可由 GM「操作紀錄」查看，不會因此自動封鎖玩家，也不記錄提交內容或憑證。

## 驗證

`test/authoritative-game.test.mjs` 與 `test/authoritative-client.test.mjs` 覆蓋真實 HTTP 拒絕本機存檔、伺服器時間與收益、重送不重複扣款、GM／交易／商城整合、重新連線冷卻、八職業創角，以及網頁被改值後恢復伺服器狀態。CLI 整合測試確認代理透過同一操作入口遊玩。
