# 存檔驗證與安全範圍

## 已有的伺服器保護

- 正式角色從登入帳號的雲端資料庫載入；本機 LocalStorage 與匯出的 JSON 不會自動取代正式角色。
- 存檔需要有效登入、CSRF、帳號操作權及最新版本。角色世代不可複製、移往另一欄或刪除後重新匯入；職業、身分及更名另有伺服器檢查。
- 藍鑽、商城卡片、交易託管與交易所收益使用伺服器帳本；GM 權限取自資料庫。

## 本版新增的防竄改規則

- 拒絕偽造 GM／交易指令序號、解除或偽造 GM 死亡、延長／新增 GM 增益，以及跳過後續 GM 傳送的序號。
- 檢查金幣、經驗、配點、HP／MP、道具數量及強化等級的格式與範圍；拒絕未知道具及同一角色背包／裝備中的重複道具識別碼。
- 新角色只能持有其職業的起始物品與配點，不能夾帶強化裝備、已學技能、萬能藥、傭兵或精通進度。
- 拒絕時整批更新回滾，原雲端角色與版本保持不變。前端停止重送同一份資料，提供「重新載入雲端存檔」按鈕。
- GM「操作紀錄」可查看最近 100 筆拒絕紀錄。資料庫最多保留 2,000 筆，同一帳號／欄位／原因每分鐘記錄一次。紀錄不包含提交的存檔內容、密碼或聊天，也不自動封鎖帳號。

## 明確限制

目前仍由瀏覽器計算戰鬥、掉寶、製作及一般商店操作。這些規則是伺服器端資料驗證，**不是完整的防作弊系統**。例如，偽造一筆格式合法、範圍正常的金幣增加或可掉落道具，仍可能通過；客戶端的遊戲計算也仍可被改寫。公開程式中的存檔校驗碼只能檢查資料毀損，不能證明資料由正常遊戲產生。

要封住上述途徑，需改成客戶端只提交操作意圖，由伺服器掌握時間、隨機數、戰鬥、獎勵及物品帳本，並拒絕客戶端直接提交角色進度。遷移範圍包括戰鬥、NPC 商店、製作、強化、倉庫、收藏、夥伴與 CLI；應先在隔離環境完整驗證功能與效能再切換正式玩家。
